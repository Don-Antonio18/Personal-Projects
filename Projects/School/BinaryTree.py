


t = makeTree(
    5,
        makeTree(
        7,
            makeEmptyTree (),
            makeTree(
            13,
                makeEmptyTree () ,
                makeEmptyTree ()
            )
        ),
    makeTree(
    11,
    
        makeEmptyTree () ,
        makeEmptyTree()
    )
)
